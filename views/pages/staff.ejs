<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS (CDN, 5.x - uses major 5 so you get latest 5.x) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5/dist/css/bootstrap.min.css" rel="stylesheet">

<!-- Site CSS (load after Bootstrap so site styles can override) -->
<link rel="stylesheet" href="/css/header.css">
<link rel="stylesheet" href="/css/main.css">
</head>
<body>
     <%- include('../layouts/header') %>
    <main class="container my-5">
        <h1 class="mb-4">Faculty & Staff</h1>

        <% if (staff && staff.length) { %>
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% staff.forEach(function(st) { %>
            <div class="col">
                <div class="card h-100">
                    <% if (st.photo) { %>
                    <img src="<%= st.photo %>" class="card-img-top" alt="<%= st.name %>">
                    <% } %>
                    <div class="card-body">
                        <h5 class="card-title"><%= st.name %></h5>
                        <h6 class="card-subtitle mb-2 text-muted"><%= st.role %></h6>
                        <p class="card-text"><%= st.bio %></p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">Email: <a href="mailto:<%= st.email %>"><%= st.email %></a></small><br>
                        <small class="text-muted">Phone: <a href="tel:<%= st.phone %>"><%= st.phone %></a></small>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
        <% } else { %>
            <p>No staff members found.</p>
        <% } %>
    </main>

    <%- include('../layouts/footer') %>
</body>
</html>